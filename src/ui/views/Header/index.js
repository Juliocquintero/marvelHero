import MarvelIcon from '../../components/MarvelIcon';
import { HeaderContainer } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <MarvelIcon />
    </HeaderContainer>
  );
};

export default Header;
